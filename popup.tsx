
function IndexPopup() {

  return (
    <div
      style={{
        padding: 16,
        width: "1000vw"
      }}>
      <h2>
        習作：カーソルを合わせたhtmlタグを拡大する(読みやすくする)
        {/* webページ内に存在するhtmlタグを全て取得し(多分配列になる) */}
        {/* それら全てにマウスイベントを設定する */}
      </h2>
    </div>
  )
}

export default IndexPopup
